let num =  Number(window.prompt("Ingrese un número"));
if(num%2==0){
    alert("\nEl número ingresado "+num+" es un número par\n");
} else{
    alert("\nEl número ingresado "+num+" no es un número par\n");
}